% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation.R
\name{plot_taxa_upset}
\alias{plot_taxa_upset}
\title{Create an UpSet Plot of Shared and Unique Taxa}
\usage{
plot_taxa_upset(
  me,
  tax_rank,
  group_var,
  plot_title = NULL,
  show_names = FALSE,
  group_labels = NULL,
  min_abundance = 0,
  min_prevalence = 0,
  filter_by_group = FALSE,
  text_size = 2,
  ...
)
}
\arguments{
\item{me}{A \code{microEDA} or \code{phyloseq} object containing OTU table, taxonomy, and sample data.}

\item{tax_rank}{\code{Character} string specifying the taxonomic rank to plot
(e.g., "Phylum", "Family"). Must be a valid rank in the taxonomy table.}

\item{group_var}{\code{Character} string indicating a sample metadata variable
to group samples.}

\item{plot_title}{Optional \code{character} string for the plot title. If \code{NULL},
a default title is generated based on \code{tax_rank}.}

\item{show_names}{\code{Logical}. If \code{TRUE}, displays taxon names on intersection
bars with italicized formatting. Default is \code{FALSE}.}

\item{group_labels}{Named character \code{vector} mapping old group names to
new labels (e.g., \code{c("Old" = "New")}).}

\item{min_abundance}{\code{Numeric} value. Minimum abundance threshold for a feature to be retained.
Must be non-negative. Features with abundance below this are considered absent.}

\item{min_prevalence}{\code{Numeric} value. Minimum prevalence required for retention.
If value is < 1, interpreted as proportion of samples; otherwise, as absolute number of samples.}

\item{filter_by_group}{\code{Logical}. If \code{FALSE} (default), filtering is applied
globally across all samples even if \code{group_var} is specified. This allows
using \code{group_var} for stratification in plotting without affecting the
filtering scope. If \code{TRUE}, filtering is applied within each group
defined by \code{group_var} and \code{group_requirement}. See \link{filter_features} for more details on filtering arguments.}

\item{text_size}{\code{Numeric}. Font size for taxon labels when
\code{show_names = TRUE}. Default is 2.}

\item{...}{Additional arguments for fine-tuning. Can include:
\itemize{
\item \code{abundance_criterion}: \code{Character} string. Criterion to use for filtering:
\describe{
\item{\code{prevalence}:}{Retain features present in at least \code{min_prevalence} samples
(within group if \code{group_var} is used) and with abundance >= \code{min_abundance}
in those samples.}
\item{\code{mean}:}{Also requires that the mean abundance across samples (or group)
is >= \code{min_abundance}.}
}
Default: \code{"prevalence"}.
\item \code{group_requirement}: \code{Character} string. When \code{group_var} is specified, determines
whether the filter criterion must be met in \code{"any"} group or \code{"all"} groups.
Default: \code{"any"}.
\item \code{keep_filtered}: Whether to keep filtered out taxa as "Other"  (Default: \code{TRUE}). Takes effect only for \code{microEDA} objects.
\item \code{rm_missing}: Whether to remove taxa with missing names (Default: \code{FALSE}).
}}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a ggplot2-based UpSet plot to visualize the intersection patterns of
taxa across different sample groups.
This provides a scalable alternative to Venn diagrams for microbiome data.
}
\details{
The function performs the following steps:
\enumerate{
\item Validates the input \code{me} object.
\item Filters taxa by \code{min_abundance}, \code{min_prevalence}.
\item Aggregates counts by \code{tax_rank}.
\item Determines taxon presence/absence per sample group.
\item Constructs an UpSet plot using the \code{ComplexUpset} package.
}

Taxon names are formatted in italics when \code{show_names = TRUE}.
}
\examples{
data(GlobalPatterns, package = "phyloseq")
plot_taxa_upset(GlobalPatterns, "Phylum", "SampleType")

}
