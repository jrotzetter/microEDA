% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_operations.R
\name{get_taxa_overlaps}
\alias{get_taxa_overlaps}
\title{Compute Taxa Overlaps and Specific Sets Across Groups}
\usage{
get_taxa_overlaps(
  me,
  tax_rank,
  group_var,
  group_labels = NULL,
  min_abundance = 0,
  min_prevalence = 0,
  abundance_criterion = c("prevalence", "mean"),
  filter_by_group = FALSE,
  group_requirement = c("any", "all"),
  keep_filtered = TRUE,
  rm_missing = FALSE,
  add_prefix = FALSE
)
}
\arguments{
\item{me}{A \code{microEDA} or \code{phyloseq} object containing OTU table, taxonomy, and sample data.}

\item{tax_rank}{\code{Character} string specifying the taxonomic rank to analyze
(e.g., "Phylum", "Family"). Must be a valid rank in the
taxonomy table.}

\item{group_var}{\code{Character} string indicating a sample metadata variable to group samples.}

\item{group_labels}{Named character \code{vector} mapping old group names to
new labels (e.g., \code{c("Old" = "New")}).}

\item{min_abundance}{\code{Numeric} value. Minimum abundance threshold for a feature to be retained.
Must be non-negative. Features with abundance below this are considered absent.}

\item{min_prevalence}{\code{Numeric} value. Minimum prevalence required for retention.
If value is < 1, interpreted as proportion of samples; otherwise, as absolute number of samples.}

\item{abundance_criterion}{\code{Character} string. Criterion to use for filtering:
\describe{
\item{\code{prevalence}:}{Retain features present in at least \code{min_prevalence} samples
(within group if \code{group_var} is used) and with abundance >= \code{min_abundance}
in those samples.}
\item{\code{mean}:}{Also requires that the mean abundance across samples (or group)
is >= \code{min_abundance}.}
}
Default: \code{"prevalence"}.}

\item{filter_by_group}{\code{Logical}. If \code{FALSE} (default), filtering is applied
globally across all samples even if \code{group_var} is specified. This allows
using \code{group_var} for stratification in without affecting the
filtering scope. If \code{TRUE}, filtering is applied within each group
defined by \code{group_var} and \code{group_requirement}. See \link{filter_features} for more details on filtering arguments.}

\item{group_requirement}{\code{Character} string. When \code{group_var} is specified, determines
whether the criterion must be met in \code{"any"} group or \code{"all"} groups.
Default: \code{"any"}.}

\item{keep_filtered}{\code{Logical}. If \code{TRUE} and any features are removed, stores the
otu_table and tax_table of the filtered-out features in the \verb{@info$filtered_taxa}
slot of \code{microEDA} objects. Has no effect for \code{phyloseq} objects.}

\item{rm_missing}{\code{Logical.} If \code{TRUE}, removes taxa with missing/unclassified entries
at the specified rank. If \code{FALSE}, fills missing values by propagating the
last known ancestor.}

\item{add_prefix}{\code{Logical}. If \code{TRUE}, adds QIIME-style prefixes (e.g., \code{k__}, \code{p__})
to taxonomic labels.}
}
\value{
A named \code{list} of character vectors where each element corresponds to a specific overlap pattern.
Empty intersection results are omitted from the final list.
}
\description{
Filters a \code{microEDA} or \code{phyloseq} object to identify present taxa per group,
then calculates all possible intersections and group-specific taxa sets.
This function is useful for identifying core vs. unique microbiome components
across conditions and can serve as an alternative to an UpSet plot.
}
\details{
This function computes taxa overlap patterns across sample groups in a microbiome dataset.
It first calls \link{get_presence_list} to obtain filtered taxa sets per group based on abundance
and prevalence criteria. It then calculates all possible set intersections and specific taxa.

\itemize{
\item \strong{Single group}: Returns only the taxa specific to that group (i.e., present in the
group and absent in all others - if others exist). If only one group is provided overall,
it simply returns that group's filtered taxa.
\item \strong{Multiple groups}: Iterates through all combination sizes from 1 to N (where N is the
number of groups). For each combination:
\itemize{
\item If the combination includes \strong{all groups}, the result is the \strong{core microbiome}
(taxa present in every group), labeled \code{"all-intersection"}.
\item If the combination includes \strong{some but not all groups}, the result includes taxa
present in \strong{all of the selected groups} and \strong{absent from all others}. These are
labeled as \code{"<group1>;...;<groupk>-intersection"}.
\item If the combination size is \strong{1}, the result includes taxa \strong{unique to that group}
(i.e., not found in any other group), labeled as \code{"<group>-specific"}.
}
}
}
\examples{
data(GlobalPatterns, package = "phyloseq")
# Calculate overlaps for Phyla across SampleTypes
overlaps <- get_taxa_overlaps(GlobalPatterns, "Phylum", "SampleType",
  min_abundance = 100, min_prevalence = 3
)

# View taxa specific to "Soil"
overlaps[["Soil-specific"]]

# View taxa shared by all groups
overlaps[["all-intersection"]]

}
\seealso{
\link{get_presence_list} for the underlying presence filtering logic.
}
