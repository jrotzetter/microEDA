% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation.R
\name{plot_taxa_sankey}
\alias{plot_taxa_sankey}
\title{Create a Sankey Plot of Taxonomic Abundances}
\usage{
plot_taxa_sankey(
  me,
  tax_rank,
  samples = NULL,
  group_var = NULL,
  plot_title = NULL,
  rm_missing = FALSE,
  group_labels = NULL,
  min_abundance = 0,
  min_prevalence = 0,
  as_relative = TRUE,
  filter_by_group = FALSE,
  text_size = 3,
  ntaxa = 30,
  ...
)
}
\arguments{
\item{me}{A \code{microEDA} or \code{phyloseq} object containing OTU table, tax_table, and optionally sample_data.}

\item{tax_rank}{\code{Character} string specifying the taxonomic rank to agglomerate at
(e.g., "genus", "family").}

\item{samples}{Optional \code{character} vector of sample names to include. If \code{NULL}, all samples are used.}

\item{group_var}{Optional name of a variable in \code{sample_data(me)} to group samples.
Used for filtering and labeling if \code{filter_by_group = TRUE}.}

\item{plot_title}{Optional \code{character} string for the plot title. If \code{NULL},
a default title is generated based on the number of samples.}

\item{rm_missing}{\code{Logical.} If \code{TRUE}, removes taxa with missing/unclassified entries
at the specified rank. If \code{FALSE}, fills missing values by propagating the
last known ancestor, labeling them as "Unclassified \emph{Last_Known_Parent_Clade}"
(e.g., "Unclassified Enterobacteriaceae"). (Default: \code{FALSE}).}

\item{group_labels}{Named character \code{vector} mapping old group names to
new labels (e.g., \code{c("Old" = "New")}).}

\item{min_abundance}{\code{Numeric} value. Minimum abundance threshold for a feature to be retained.
Must be non-negative. Features with abundance below this are considered absent.}

\item{min_prevalence}{\code{Numeric} value. Minimum prevalence required for retention.
If value is < 1, interpreted as proportion of samples; otherwise, as absolute number of samples.features.}

\item{as_relative}{\code{Logical}. If \code{TRUE}, applies relative abundance transformation (TSS)
to the input counts. If \code{FALSE}, uses raw counts. (Default: \code{TRUE}).}

\item{filter_by_group}{\code{Logical}. If \code{FALSE} (default), filtering is applied
globally across all samples even if \code{group_var} is specified. This allows
using \code{group_var} for stratification in plotting without affecting the
filtering scope. If \code{TRUE}, filtering is applied within each group
defined by \code{group_var} and \code{group_requirement}. See \link{filter_features} for more details on filtering arguments.}

\item{text_size}{Label size of taxa names (Default: 3).}

\item{ntaxa}{Number of top taxa to display before grouping into "Other" (Default: 30).}

\item{...}{Additional arguments for fine-tuning. Can include:
\itemize{
\item \code{abundance_criterion}: \code{Character} string. Criterion to use for filtering:
\describe{
\item{\code{prevalence}:}{Retain features present in at least \code{min_prevalence} samples
(within group if \code{group_var} is used) and with abundance >= \code{min_abundance}
in those samples.}
\item{\code{mean}:}{Also requires that the mean abundance across samples (or group)
is >= \code{min_abundance}.}
}
Default: \code{"prevalence"}.
\item \code{group_requirement}: \code{Character} string. When \code{group_var} is specified, determines
whether the filter criterion must be met in \code{"any"} group or \code{"all"} groups.
Default: \code{"any"}.
\item \code{keep_filtered}: Whether to keep filtered out taxa as "Other"  (Default: \code{TRUE}). Takes effect only for \code{microEDA} objects.
\item \code{ncp}: Number of control points on the Bezier curve that forms the edge.
Larger numbers will result in smoother curves, but cost more computational time. (Default: 100).
\item \code{slope}: Slope parameter for the Bezier curves used to depict the edges.(Default: 0.5).
}}
}
\value{
A \code{ggplot} object displaying the Sankey diagram.
}
\description{
Visualizes the flow of taxa across hierarchical taxonomic ranks (e.g.,
Kingdom -> Phylum -> Class -> Order -> Family -> Genus -> Species) using a Sankey diagram.
If multiple samples are provided or \code{samples = NULL} (default), mean abundances
across samples are used. The number of displayed taxa is limited to \code{ntaxa}
to avoid visual clutter.
}
\examples{
data(GlobalPatterns, package = "phyloseq")
plot_taxa_sankey(GlobalPatterns, tax_rank = "Order", samples = "Even3")

}
