% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering.R
\name{filter_features}
\alias{filter_features}
\title{Filter Features in a microEDA Object Based on Abundance and Prevalence}
\usage{
filter_features(
  me,
  min_abundance = 0,
  min_prevalence = 0,
  group_var = NULL,
  abundance_criterion = c("prevalence", "mean"),
  group_requirement = c("any", "all"),
  keep_other = TRUE
)
}
\arguments{
\item{me}{A \code{microEDA} object containing microbiome abundance data and associated metadata.}

\item{min_abundance}{\code{Numeric} value. Minimum abundance threshold for a feature to be retained.
Must be non-negative. Features with abundance below this are considered absent.}

\item{min_prevalence}{\code{Numeric} value. Minimum prevalence required for retention.
If value is < 1, interpreted as proportion of samples; otherwise, as absolute number of samples.}

\item{group_var}{(optional) Name of the sample metadata variable to define groups
for stratified filtering. If \code{NULL}, filtering is applied across all samples.}

\item{abundance_criterion}{\code{Character} string. Criterion to use for filtering:
\describe{
\item{\code{prevalence}:}{Retain features present in at least \code{min_prevalence} samples
(within group if \code{group_var} is used) and with abundance >= \code{min_abundance}
in those samples.}
\item{\code{mean}:}{Also requires that the mean abundance across samples (or group)
is >= \code{min_abundance}.}
}
Default: \code{"prevalence"}.}

\item{group_requirement}{\code{Character} string. When \code{group_var} is specified, determines
whether the criterion must be met in \code{"any"} group or \code{"all"} groups.
Default: \code{"any"}.}

\item{keep_other}{\code{Logical}. If \code{TRUE} and any features are removed, adds a new row
named \code{"Other"} to the OTU and tax tables, summarizing the total abundance of
all filtered features. Existing \code{"Other"} rows are merged.}
}
\value{
A new \code{microEDA} object with filtered features. A message reports how many
features were removed. If \code{keep_other = TRUE} and features were removed,
an \code{"Other"} row is added or updated.
}
\description{
Filters taxa (features) from a \code{microEDA} object based on minimum abundance
and prevalence thresholds. Filtering can be applied globally or within groups
defined by a metadata variable. Optionally adds an "Other" category summarizing
filtered features.
}
\examples{
data(GlobalPatterns, package = "phyloseq")
me <- microEDA(GlobalPatterns)

# Filter features present in at least 10\% of samples with an abundance >= 100
filtered_me <- filter_features(me, min_abundance = 100, min_prevalence = 0.1)

# Stratified filtering: feature must meet criteria in all groups, that is
# in at least 2 samples per group with an abundance of >= 10
filtered_me <- filter_features(
  me,
  group_var = "SampleType",
  min_abundance = 10,
  min_prevalence = 2,
  group_requirement = "all"
)

}
