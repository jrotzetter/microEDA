% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_taxonomy_prefix.R
\name{add_taxonomy_prefix}
\alias{add_taxonomy_prefix}
\title{Add taxonomic prefixes to tax_table columns}
\usage{
add_taxonomy_prefix(
  me,
  prefix_map = c(Kingdom = "k__", Phylum = "p__", Class = "c__", Order = "o__", Family =
    "f__", Genus = "g__", Species = "s__", Strain = "t__")
)
}
\arguments{
\item{me}{A \code{character} matrix, \code{data.frame}, \code{microEDA} or \code{phyloseq}-class
object containing a \code{tax_table}. If a \code{microEDA} or \code{phyloseq} object is passed,
its internal taxonomyTable is modified.}

\item{prefix_map}{A named \code{character} vector mapping taxonomic rank names
(e.g., "Kingdom", "Phylum") to their corresponding prefixes (e.g., "k__", "p__").
Defaults to standard QIIME-style prefixes for common ranks including "Strain" ("t__").}
}
\value{
Returns the input object with prefixes added to appropriate cells:
\itemize{
\item For matrices/data.frames: returns the modified matrix or data.frame.
\item For \code{microEDA}/\code{phyloseq} objects: returns the updated \code{microEDA}/\code{phyloseq}
object with prefixed \code{tax_table}.
}
}
\description{
Adds QIIME-style two-underscore prefixes (e.g., "k__", "p__") to values in a
taxonomy matrix or data.frame if they are not already present. The function
is idempotent, meaning it will not add duplicate prefixes when reapplied.
}
\details{
This function supports both character matrices/data.frames and \code{microEDA} or
\code{phyloseq} objects.
It only modifies columns matching known taxonomic ranks (as defined in \code{prefix_map}),
leaving other columns and missing/empty values (\code{NA}, "", " ") unchanged.
\itemize{
\item Only columns in \code{me} that match keys in \code{prefix_map} are processed.
\item Values already starting with any valid prefix (e.g., "k__Bacteria") are skipped.
\item Empty strings (\code{""}), \code{NA}, and whitespace-only entries are preserved without modification.
}
}
\examples{
# Example 1: Basic usage with data.frame
tax_data <- data.frame(
  Kingdom = c("Bacteria", "Archaea"),
  Phylum = c("Proteobacteria", "Euryarchaeota"),
  Genus = c("Escherichia", "Methanobrevibacter"),
  Species = c("Escherichia coli", "Methanobrevibacter smithii"),
  stringsAsFactors = FALSE
)

prefixed <- add_taxonomy_prefix(tax_data)
prefixed

# Example 2: Idempotent — applying again changes nothing
add_taxonomy_prefix(prefixed)

# Example 3: Basic usage with phyloseq objects
data("GlobalPatterns", package = "phyloseq")
GlobalPatterns <- add_taxonomy_prefix(GlobalPatterns)
head(phyloseq::tax_table(GlobalPatterns)[, c("Kingdom", "Phylum", "Order")])

}
\seealso{
\code{\link[=trim_taxonomy_prefix]{trim_taxonomy_prefix()}} to remove prefixes.

\code{\link[phyloseq:tax_table-methods]{phyloseq::tax_table()}} to access taxonomy data.
}
