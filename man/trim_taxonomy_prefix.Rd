% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_taxonomy_prefix.R
\name{trim_taxonomy_prefix}
\alias{trim_taxonomy_prefix}
\title{Remove taxonomic prefixes from tax_table columns}
\usage{
trim_taxonomy_prefix(
  me,
  prefix_map = c(Kingdom = "k__", Phylum = "p__", Class = "c__", Order = "o__", Family =
    "f__", Genus = "g__", Species = "s__", Strain = "t__")
)
}
\arguments{
\item{me}{A \code{character} matrix, \code{data.frame}, \code{microEDA} or a \code{phyloseq}-class
object containing a \code{tax_table}. If a \code{microEDA} or \code{phyloseq} object is
passed, its taxonomyTable is modified.}

\item{prefix_map}{A named \code{character} vector mapping taxonomic rank names
(e.g., "Kingdom", "Phylum") to their corresponding prefixes (e.g., "k__", "p__").
Defaults to standard QIIME-style prefixes including "Strain" ("t__").}
}
\value{
Returns the input object with prefixes removed:
\itemize{
\item For matrices/data.frames: returns a modified \code{character} matrix or data.frame.
\item For \code{microEDA}/\code{phyloseq} objects: returns the updated \code{microEDA}/\code{phyloseq} object with cleaned \code{tax_table}.
}
}
\description{
Removes QIIME-style two-underscore prefixes (e.g., "k__", "p__") from values
in a taxonomy matrix or data.frame if present. The function is idempotent:
repeated application has no additional effect.
}
\details{
Supports character matrices, data.frames, \code{microEDA} and \code{phyloseq} objects.
Only modifies cells in columns matching the keys of \code{prefix_map} and only when
the value starts with the corresponding prefix. Missing (\code{NA}), empty, or
unprefixed values are left unchanged.
\itemize{
\item Only columns in \code{me} that match names in \code{prefix_map} are processed.
\item Prefix removal is exact: e.g., "k__" is only removed from "Kingdom" column if it starts the value.
\item Uses efficient string processing (e.g., via \code{stringi}) to detect and remove prefixes.
\item Preserves \code{NA}, \code{""}, and whitespace-only entries.
}
}
\examples{
# Example 1: Basic usage with data.frame
prefixed_tax_data <- data.frame(
  Kingdom = c("k__Bacteria", "k__Archaea"),
  Genus = c("g__Escherichia", "g__Methanobrevibacter"),
  Species = c("s__Escherichia coli", "s__Methanobrevibacter smithii"),
  stringsAsFactors = FALSE
)
prefixed_tax_data
trimmed <- trim_taxonomy_prefix(prefixed_tax_data)
trimmed

# Example 2: Idempotent — applying again changes nothing
trim_taxonomy_prefix(trimmed)

# Example 3: Basic usage with phyloseq objects
data("GlobalPatterns", package = "phyloseq")
GlobalPatterns <- add_taxonomy_prefix(GlobalPatterns)
head(phyloseq::tax_table(GlobalPatterns)[, c("Kingdom", "Phylum", "Order")])

GlobalPatterns <- trim_taxonomy_prefix(GlobalPatterns)
head(phyloseq::tax_table(GlobalPatterns)[, c("Kingdom", "Phylum", "Order")])

}
\seealso{
\code{\link[=add_taxonomy_prefix]{add_taxonomy_prefix()}} to add prefixes.

\code{\link[phyloseq:tax_table-methods]{phyloseq::tax_table()}} to access taxonomy data.
}
