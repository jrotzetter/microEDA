% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualisation.R
\name{plot_composition}
\alias{plot_composition}
\title{Plot Taxonomic Composition of a microEDA/phyloseq Object}
\usage{
plot_composition(
  me,
  tax_rank,
  group_var = NULL,
  facet_var = NULL,
  plot_title = NULL,
  group_labels = NULL,
  show_samples = TRUE,
  min_abundance = 0,
  min_prevalence = 0,
  as_relative = TRUE,
  filter_by_group = FALSE,
  ...
)
}
\arguments{
\item{me}{A \code{microEDA} or \code{phyloseq} object containing OTU table, taxonomy, and sample data.}

\item{tax_rank}{\code{Character} string specifying the taxonomic rank to plot
(e.g., "Phylum", "Family"). Must be a valid rank in the
taxonomy table.}

\item{group_var}{\code{Character} string (optional) indicating a sample metadata
variable to group samples.}

\item{facet_var}{\code{Character} string (optional) indicating a metadata variable
for additional faceting/grouping of \code{group_var}.}

\item{plot_title}{\code{Character} string for the main plot title.}

\item{group_labels}{Named character \code{vector} mapping old group names to
new labels (e.g., \code{c("Old" = "New")}).}

\item{show_samples}{\code{Logical}. Control sample label visibility.}

\item{min_abundance}{\code{Numeric} value. Minimum abundance threshold for a feature to be retained.
Must be non-negative. Features with abundance below this are considered absent.}

\item{min_prevalence}{\code{Numeric} value. Minimum prevalence required for retention.
If value is < 1, interpreted as proportion of samples; otherwise, as absolute number of samples.}

\item{as_relative}{\code{Logical}. If \code{TRUE}, applies relative abundance transformation (TSS)
to the input counts. If \code{FALSE}, uses raw counts. (Default: \code{TRUE}).}

\item{filter_by_group}{\code{Logical}. If \code{FALSE} (default), filtering is applied
globally across all samples even if \code{group_var} is specified. This allows
using \code{group_var} for stratification in plotting without affecting the
filtering scope. If \code{TRUE}, filtering is applied within each group
defined by \code{group_var} and \code{group_requirement}. See \link{filter_features} for more details on filtering arguments.}

\item{...}{Additional arguments for fine-tuning. Can include:
\itemize{
\item \code{sample_text}: Font size for sample labels (Default: 8).
\item \code{strip_angle}: Angle of facet strip text (Default: 90).
\item \code{legend_text}: Font size for legend (Default: 9).
\item \code{legend_key}: Size of legend keys (Default: 8).
\item \code{bar_width}: Width of bars (Default: 0.95).
\item \code{ntaxa}: Number of top taxa to display (Default: 30).
\item \code{pal}: Color palette name (passed to \code{RColorBrewer}, default: "Paired").
Must be a valid palette name with at least 12 colors.
\item \code{abundance_criterion}: \code{Character} string. Criterion to use for filtering:
\describe{
\item{\code{prevalence}:}{Retain features present in at least \code{min_prevalence} samples
(within group if \code{group_var} is used) and with abundance >= \code{min_abundance}
in those samples.}
\item{\code{mean}:}{Also requires that the mean abundance across samples (or group)
is >= \code{min_abundance}.}
}
Default: \code{"prevalence"}.
\item \code{group_requirement}: \code{Character} string. When \code{group_var} is specified, determines
whether the filter criterion must be met in \code{"any"} group or \code{"all"} groups.
Default: \code{"any"}.
\item \code{keep_filtered}: Whether to keep filtered out taxa as "Other"  (Default: \code{TRUE}).
\item \code{rm_missing}: Whether to remove taxa with missing names  (Default: \code{FALSE}).
\item \code{process_taxon}: Whether to clean taxon names (remove underscores, return in italic). (Default: \code{TRUE}).
}}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a ggplot2-based stacked bar chart showing the abundance of taxa
across samples, optionally grouped and faceted by metadata variables.
}
\details{
\itemize{
\item The function prepares the taxonomic profile by:
\itemize{
\item Aggregating counts by \code{tax_rank}.
\item Filtering taxa by \code{min_abundance}, \code{min_prevalence}, and reducing
the total number of taxa to no more than \code{ntaxa}.
\item Optionally grouping and transforming data.
}
\item If \code{group_var} is specified, samples are grouped by that variable.
\item If \code{facet_var} is provided, an additional \code{facet_wrap} layer is added with markdown formatting.
\item Sample counts per group are appended to group labels (e.g., "\strong{Control (n=10)}").
}
}
\examples{
# Basic plot at Species level
mpa <- microEDA(merged_metaphlan_profiles)
plot_composition(mpa, "Species")

}
